part of tyba.core.services;

class TripsService {
  // Api _api = locator<Api>();

  // List<Trip> _trips;
  // Map<String, List<TripEvent>> _routes;
  // List<TripPlace> _places;
  // TripPlace viewedPlace;
  // List<TripMap> _maps;

  // /// [GETTER]
  // List<Trip> get trips => _trips;
  // Map<String, List<TripEvent>> get routes => _routes;
  // List<TripPlace> get places => _places;
  // List<TripMap> get maps => _maps;

  // /// [API Request]
  // Future getTripsForUser() async {
  //   _trips = await _api.getTripsForUser();
  // }

  // Future getTripRoutes(String tripId) async {
  //   _routes = await _api.getTripRoutes(tripId);
  // }

  // void resetPlaces() {
  //   _places = List();
  // }
  // void resetTrips() {
  //   _trips = List();
  // }

  // void resetMaps() {
  //   _maps = List();
  // }

  // void cleanViewedPlace() {
  //   viewedPlace = null;
  // }

  // Future getRoutePlaces(String tripId, String routeId) async {
  //   _places = await _api.getRoutePlaces(tripId, routeId);
  // }

  // Future getTripPlace(String tripId, String routeId) async {
  //   viewedPlace = await _api.getTripPlace(tripId, routeId);
  // }

  // Future getTripMap(String tripId) async {
  //   _maps = await _api.getTripMaps(tripId);
  // }
  // Future removeTripMap(String tripId) async {
  //   _maps = await _api.removeMaps(tripId);
  // }
  // Future getItinerary(String tripId) async {
  //   return await _api.downloadItinerary(tripId);
  // }
  // Future getItineraryPlaces(String tripId) async {
  //   return await _api.downloadPlaces(tripId);
  // }
  // Future<List<File>> getItineraryImages(String tripId) async {
  //   return await _api.downloadMediaContent(tripId);
  // }
}
